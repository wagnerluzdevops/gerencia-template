import { environment } from '../environments/environment';
export const APP_API = environment.api;
export const APP_OAUTH = environment.oauth;
export const APP_OAUTH_REVOKE = environment.oauthrevoke;
export const ASSETSPATH = environment.assetspath;
export const URL_UPLOAD = environment.urlupload;

